1. What is the maximum GPU bandwidth you observed? For which problem sizes? 
   nx         | blockSize | trial | MLUP/s       | Bandwidth (GB/s)
   131072     | 256       | 3     | 49468.60     | 791.4976   

2. How does it compare to the theoretical maximum published by the vendor?
448 GB/s
This bandwidth is achieved using 8 GB of GDDR6 memory on a 256-bit bus, 
running at an effective clock speed of 14 Gbps.
Our average is similar (406.32), but maximum is much more higher.  
(may because of cache?)

cache effect: cache is filled around 1MB. 

3. How does the GPU bandwidth relate to the CPU bandwidth?
15-16x faster than serial CPU and about 8x faster than OpenMP CPU. 

4. Can you identify problem sizes that are better suited for CPU or for GPU?
CPU: nx=4096(32KB)
GPU: over 1 MB

5. What CUDA block size did you choose? How did you make that choice?
256 according to plot. Larger block size might cause more cache misses. 

6. How did you map CUDA threads to image pixels?
px = blockId.x * blockDim.x + threadId.x
px = blockId.y * blockDim.y + threadId.y
Threads are mapped one to one image pixels using 2d grid. 

7. How much (global) memory is accessed by each thread? 
RGBA each 1 byte, in total 4 bytes each thread. 

8. How many computations are done by each thread? Does this kernel show 
a different balance (bytes / flop) than the one from the previous exercise?
 - iterative update loop z_(n+1) = z_n^2 + c (expensive)
 - color mapping 
 Yes, this kernal performs more arithmetric operations per thread. Lower bytes/flop ratio. 

9. How can you find out what the bottleneck of your kernel is?
Profiling tools (nsight) to see memory throughput or instruction throughput is higher. 